# 手动编译与发布指导

> 项目：题迹（tage，Electron + electron-vite + electron-builder）
> 产物命名：`tage-{版本号}-x64.exe`（由 `package.json` 的 `name` + `version` 决定）

## 一、核心认知（最重要）

- `pnpm dev` 跑的是 `src/` 源码；`pnpm build` 把源码编译到 `out/`；`pnpm dist` 只把 `out/` 打包成安装包，**不会重新编译源码**。
- 所以改完代码必须 **先 `pnpm build` 再 `pnpm dist`**，否则装进去的是旧 `out/` 产物，会出现“dev 正常、安装包是旧内容”的灵异现象。
- `electron-updater` 比较的是 `package.json` 的 `version`。**不升版本号，发布后老用户不会收到自动更新**；同时 GitHub 上同 tag 的 release 已发布超过 2 小时会被 electron-builder 跳过。
- PowerShell 读环境变量用 `$env:GH_TOKEN`，不是 `${GH_TOKEN}`（后者是 PowerShell 普通变量，永远空）。
- 系统环境变量新增后，**已打开的终端 / VSCode 集成终端不会自动刷新**，需新开终端或彻底退出 VSCode 重开。

## 二、标准发布流程（发布到 GitHub，触发自动更新）

```powershell
# 0. 进入项目目录
cd D:\Desktop\Node\Comate

# 1. 升版本号：编辑 package.json，把 "version": "0.1.6" 改成新版本（如 0.1.7）
#    每次发布都必须升，否则 electron-updater 不触发、且会撞已存在的 release

# 2. 编译源码到 out/（不可省）
pnpm build

# 3. 设置镜像（避免从 GitHub 下载 electron-builder 二进制超时）
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"

# 4. 设置 GitHub Token（发布必需）
$env:GH_TOKEN="你的_github_personal_access_token"

# 5. 打包并发布：生成 exe + latest.yml，上传到 GitHub release v{版本号}
pnpm dist
```

产物：
- 安装包：`dist\tage-{版本号}-x64.exe`
- 自动更新清单：`dist\latest.yml`（electron-updater 依赖）
- 解压目录：`dist\win-unpacked\`

发布成功后，已安装旧版本的用户启动软件时会自动检测并下载新版本。

## 三、仅本地打包，不发布到 GitHub

用于本地验证、测试安装包内容是否正确。

```powershell
cd D:\Desktop\Node\Comate
pnpm build
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"

# 方式 A：只生成解压目录（最快，不生成 exe）
pnpm dist:dir

# 方式 B：生成 exe 但不上传 GitHub
pnpm exec electron-builder --publish never
```

> 也可把 `package.json` 里 `"dist": "electron-builder --publish always"` 改成 `--publish never`，避免每次都要求 `GH_TOKEN`。

## 四、GH_TOKEN 配置

1. GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens → 勾选仓库 `bugsong/electron-tage`，权限 `Contents: Read and write` → 生成。
2. 临时（仅当前终端有效）：
   ```powershell
   $env:GH_TOKEN="github_pat_xxxxx"
   ```
3. 永久（写入用户环境变量，需新开终端才生效）：
   ```powershell
   [Environment]::SetEnvironmentVariable("GH_TOKEN","github_pat_xxxxx","User")
   ```
   设置后**新开 PowerShell 窗口**或**彻底退出 VSCode 重开**，再用 `$env:GH_TOKEN` 验证非空。

## 五、GitHub release 冲突处理

发布时常见报错与处理：

| 报错 | 原因 | 处理 |
|------|------|------|
| `existing release published more than 2 hours ago` | 同版本号 release 已发布超 2 小时，electron-builder 不覆盖 | 升 `package.json` 版本号后重新 `pnpm build && pnpm dist` |
| `already_exists` (tag_name) | GitHub 上已存在该 tag/release（上次发布半途残留） | 删掉远端 release + tag 后重发，或直接升版本号 |
| `Published releases must have a valid tag` | 创建 published release 时自动建 tag 失败（token 权限或删除后最终一致性延迟） | 先单独建 tag，或等几十秒重试，或升版本号 |

**清理远端残骸 release + tag（PowerShell，需 `$env:GH_TOKEN`）：**

```powershell
$repo = "bugsong/electron-tage"
$tag  = "v0.1.6"   # 改成你要清理的 tag
$h = @{ Authorization = "Bearer $env:GH_TOKEN"; Accept = "application/vnd.github+json" }

# 1. 删 release
try {
  $rel = Invoke-RestMethod "https://api.github.com/repos/$repo/releases/tags/$tag" -Headers $h
  Invoke-RestMethod -Method Delete "https://api.github.com/repos/$repo/releases/$($rel.id)" -Headers $h | Out-Null
  "release deleted"
} catch { "release: $($_.Exception.Message)" }

# 2. 删 tag ref
try {
  Invoke-RestMethod -Method Delete "https://api.github.com/repos/$repo/git/refs/tags/$tag" -Headers $h | Out-Null
  "tag deleted"
} catch { "tag: $($_.Exception.Message)" }

# 3. 等 GitHub 最终一致（删除有延迟），约 30 秒后重试 pnpm dist
```

> 删除后 GitHub 状态有短暂最终一致性延迟，立刻重发可能仍报 `already_exists` / `must have a valid tag`，等 30~60 秒再 `pnpm dist`。
>
> 最省事的办法永远是：**升一个版本号**，避开所有残骸。

**补传缺失资产**：若 release 已创建但 `exe`/`latest.yml` 上传中断，只要 release 发布在 2 小时内，直接再跑 `pnpm dist` 即可补传。

## 六、文件占用处理（EBUSY: app.asar 等）

打包时 `dist\` 下文件被占用，通常是应用还在运行或资源管理器锁定。

```powershell
# 1. 结束所有 electron / 题迹 进程
Get-Process -Name electron,"题迹" -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 2

# 2. 清理 dist
Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue

# 3. 仍被锁定时，换输出目录绕过
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
pnpm exec electron-builder --publish never --config.directories.output=dist2
```

## 七、常见问题速查

| 问题 | 解决 |
|------|------|
| dev 正常但安装包是旧内容 | 改完源码没 `pnpm build` 就 `pnpm dist`；先 `pnpm build` 再 `pnpm dist` |
| 老用户收不到自动更新 | 没升 `package.json` 的 `version`；每次发布必须升版本号 |
| `GH_TOKEN is not set` | `$env:GH_TOKEN="..."`；永久设置后需新开终端 / 重启 VSCode |
| `${GH_TOKEN}` 为空 | PowerShell 环境变量要用 `$env:GH_TOKEN` |
| `connect ETIMEDOUT 20.205.243.166` | 设置 `ELECTRON_BUILDER_BINARIES_MIRROR` 镜像 |
| `ERR_PNPM_IGNORED_BUILDS` | 编辑 `pnpm-workspace.yaml`，把对应包设为 `true` |
| `EBUSY: app.asar` | 关闭运行中的应用 / 资源管理器，或换 `--config.directories.output=dist2` |
| release `already_exists` / `must have a valid tag` | 清理远端残骸或升版本号重发 |

## 八、一键发布（可选）

把标准流程串起来，改完版本号后直接跑：

```powershell
cd D:\Desktop\Node\Comate
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
# $env:GH_TOKEN 已在用户环境变量中
pnpm build; if ($?) { pnpm dist }
```
