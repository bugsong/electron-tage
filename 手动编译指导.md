思考过程
Read
package.json
L:1 - 5
思考过程

版本号已是 0.1.1。开始编译：
编译主进程和渲染进程
自动
pnpm build
打包到本地dist目录
自动
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"; pnpm dist:dir
思考过程

文件被占用，清理 dist 后重试：
清理dist后重新打包
自动
Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue; $env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"; pnpm dist:dir
思考过程

有进程占用文件，先结束所有 electron 进程：
结束electron进程并清理dist
自动
Get-Process -Name electron,"题迹" -ErrorAction SilentlyContinue | Stop-Process -Force; Start-Sleep -Seconds 2; Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue
重新打包
自动
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"; pnpm dist:dir
思考过程
检查dist目录状态
自动
if (Test-Path dist) { Get-ChildItem -Recurse dist | Select-Object FullName } else { "dist not exists" }
思考过程

app.asar 被锁定。强制删除后重试：
强制删除被锁定的app.asar
自动
Remove-Item -Force "D:\Desktop\Node\Comate\dist\win-unpacked\resources\app.asar" -ErrorAction Stop
思考过程
查找node和electron进程
自动
Get-Process node,electron -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,Path,StartTime
思考过程

可能是 Defender 或资源管理器锁定。换输出目录绕过：
换dist2目录打包
自动
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"; pnpm exec electron-builder --dir --out dist2
思考过程
用配置覆盖输出目录
自动
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"; pnpm exec electron-builder --dir -c.directories.output=dist2
思考过程
用引号覆盖输出目录
自动
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"; pnpm exec electron-builder --dir "--config.directories.output=dist2"
思考过程

解压目录已生成。现在生成 NSIS 安装包（不发布）：
生成NSIS安装包不发布
自动
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"; pnpm exec electron-builder --publish never "--config.directories.output=dist2"
思考过程

编译成功！确认产物：
查看生成的exe文件
自动
Get-ChildItem dist2 -Filter "*.exe" | Select-Object Name,Length,LastWriteTime
思考过程

编译成功！产物：dist2\comate-0.1.1-x64.exe（约 122 MB）。

手动编译命令笔记
1. 改版本号

编辑 package.json，修改 "version": "x.x.x"。

2. 编译 + 打包（PowerShell）
powershell
 
# 进入项目目录
cd D:\Desktop\Node\Comate

# 设置镜像（避免从 GitHub 下载超时）
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"

# 编译主进程/渲染进程/preload
pnpm build

# 打包生成 exe（不自动发布到 GitHub）
pnpm exec electron-builder --publish never "--config.directories.output=dist2"

3. 产物
安装包：dist2\comate-{版本号}-x64.exe
解压目录：dist2\win-unpacked\
4. 手动发布到 GitHub
powershell
 
# 打 tag 并推送
git tag v{版本号}
git push origin v{版本号}

# 然后在 GitHub 仓库手动创建 Release，上传 dist2\comate-{版本号}-x64.exe
# 同时上传 latest.yml（electron-updater 自动更新需要，位于 dist2\）

常见问题
问题	解决
ERR_PNPM_IGNORED_BUILDS	编辑 pnpm-workspace.yaml，把对应包设为 true
connect ETIMEDOUT 20.205.243.166	设置上面的镜像环境变量
EBUSY: app.asar	关闭运行中的应用/资源管理器，或用 --config.directories.output=dist2 换输出目录
任务完成
共消耗: 560.6K
3min 57s