# 刷题笔记 PRD（v1.0）

> 本文档是后续开发的唯一依据。每次功能变更需同步更新本文件。

---

## 1. 产品目标和使用场景

**产品目标**：为个人刷题学习提供本地离线工具，实现刷题记录、错题复盘、随题笔记、草稿演算和数据管理，数据仅存储在本机，不登录、不联网、不上云。

**使用场景**：
- 在电脑上随时打开，做题、整理错题、写笔记
- 通过 Excel 批量导入上万道题库，日常刷题练习
- 交卷后统一判分，答错的题自动进入错题本，后续针对性重练
- 任何一道题目的草稿笔迹（草纸演算）可随时复用，下次打开题目时完整恢复
- 刷新、关闭、重启电脑后，所有数据（进度、笔记、笔迹）完整保留

**技术边界**：仅在 Windows 电脑本地运行，不做手机端、不做登录、不做联网、不做云数据库。

---

## 2. 整体页面框架和导航结构

```
┌──────────┬──────────────────────────────────┐
│ 侧边栏    │  主内容区（当前模块页面）           │
│          │                                  │
│ 🏠 首页   │                                  │
│ 📚 专项练习│                                  │
│ ❌ 错题   │                                  │
│ 📝 笔记   │                                  │
│ ⭐ 收藏   │                                  │
│ 🗂 题目管理│                                  │
│ ⚙ 设置   │                                  │
└──────────┴──────────────────────────────────┘
```

- 左侧固定侧边栏，宽度 13rem，当前项浅蓝底白字高亮
- 主内容区：固定 padding，右上角快捷操作按钮统一放在页面内部
- 全局浮层（蒙层弹窗）：PaperCanvas 草纸浮层、各类确认弹窗、富文本笔记编辑器
- 配色/字号通过 CSS 变量全局注入，`data-theme="light|dark"` + `data-font-size`，所有页面即时生效

### 路由清单

| 路由 | 页面 |
|---|---|
| `/home` | 首页总览 |
| `/practice` | 专项练习（目录树） |
| `/practice/session` | 专项智能练习（做题页） |
| `/practice/result` | 交卷结果页 |
| `/wrong` | 错题 |
| `/notes` | 笔记汇总 |
| `/favorites` | 收藏 |
| `/questions` | 题目管理 |
| `/settings` | 设置 |

做题页、结果页三个模块共用（专项/错题重练/收藏练习），只是数据来源、标题、交卷后额外动作不同。

---

## 3. 首页的内容结构

```
┌──────────────────────────────────────────────┐
│ 你好 · 今天也要好好刷题          [继续上次练习]   │
│ 2026年8月25日                              │
├───────────┬───────────┬───────────┬──────────┤
│ 累计答题   │ 正确率     │ 今日答题   │ 错题数     │
│  1,234    │  68.5%    │   56      │   78     │
├───────────┴───────────┴───────────┴──────────┤
│ 专项练习进度                                    │
│ ▸ 政治理论      ██████░░ 480/800  [去练习]     │
│ ▸ 常识判断      ████░░░░ 320/1200 [去练习]     │
│ …（每个一级分类一行，点击进入目录页并展开）          │
├──────────────────────────────────────────────┤
│ [错题重练] 待复习错题 78 道   │ [自定义组卷]       │
├──────────────────────────────────────────────┤
│ 最近笔记（最新 5 条，点击跳转到对应题目）              │
└──────────────────────────────────────────────┘
```

- 统计数据来自 `question_progress` 聚合计算
- 专项进度 = 该分类「已做题数 / 总题数」，点击进入 `/practice` 并自动展开对应分类
- 「继续上次练习」按钮：仅当存在进行中会话时显示，点击恢复题目顺序、已选答案、累计用时
- 快捷操作区：错题重练、自定义组卷、导入题目、我的笔记

---

## 4. 每个模块的用途

### 4.1 首页总览
- 查看总览数据：累计答题数、正确率、今日答题数、错题数
- 快速进入各专项练习进度列表，点击可跳转并展开对应分类
- 快速发起「继续上次练习」「错题重练」「自定义组卷」
- 查看最近 5 条笔记摘要，点击跳转到该题详情

### 4.2 专项练习
- 树形折叠目录：政治理论下 8 个子项 + 5 个一级分类，每行显示「已做/总题数」
- 点击「去练习」→ 随机抽取 20 道题，进入做题页
- 点击「自定义刷题」→ 自定义组卷弹窗：自选分类/子分类组合 + 题量（10/20/30/50）+ 题型
- 政治理论下的「全部」为虚拟节点，代表整个一级分类

### 4.3 做题页（专项/错题重练/收藏练习共用）
- 顶部：返回、计时（进入即开始）、居中标题、设置按钮、交卷按钮
- 每张题目卡片：
  - 题号 + 【单选题】蓝色标签
  - 右上角三个图标：**📝随题笔记**（富文本编辑器弹窗）**📄草纸**（PaperCanvas 浮层）**⭐收藏**（星标切换）
  - 错题重练来源时追加第四个图标：**🗑移出错题本**（仅对当前题目）
  - 大段题干 + A/B/C/D 选项，圆形单选框
- 每选一题、每过 10 秒自动通过 IPC 保存进度到 SQLite
- 点击返回 = 保存进度后离开，不弹确认（进度保留，下次可继续）
- 交卷弹确认框（显示已答/未答数）→ 统一判分 → 跳结果页
- 交卷后答错的题自动写入错题本

### 4.4 交卷结果页
- 顶部汇总卡：得分、正确率、用时、答对/答错/未答数
- 逐题对错列表（题号 + ✅/❌ + 题干摘要 + 我的答案/正确答案）
- 点击某题 → 详情弹窗：题干/选项/答案解析/草纸/笔记
- 底部：返回目录 / 错题重练（若答错题 > 0）

### 4.5 错题
- 顶部：错题总数 +「错题重练」按钮 + 分类筛选 + 搜索框
- 列表：题目摘要 + 分类 + 答错次数 + 最近答错时间 + 操作（复盘 / 移除）
- 复盘：详情弹窗（题干选项 + 答错时的选择 + 正确答案 + 解析 + 草纸旧笔迹 + 笔记），可「标记已掌握」（移出错题本）
- 错题重练：从错题本随机抽题，题量默认 20，可选分类筛选
- 错题重练答对**不自动移出**，需手动「标记已掌握」

### 4.6 笔记
- 汇总浏览页：全部笔记按更新时间倒序，每条显示笔记摘要 + 所属题目摘要 + 分类 + 更新时间，点击跳转到该题详情
- 做题/结果/错题复盘/收藏详情中均可在题目卡片右上角点「📝」写笔记
- 笔记编辑器：contenteditable 富文本（加粗/斜体/下划线/删除线/标题/列表/引用/清除格式），**停止输入约 1 秒后自动保存**，HTML 格式存入 SQLite

### 4.7 收藏
- 列表：题目摘要 + 分类 + 收藏时间 + 查看入口 + 取消收藏
- 「收藏练习」：从收藏题随机抽题组卷
- 详情弹窗：题干/选项/答案解析/草纸/笔记

### 4.8 题目管理
- 顶部：搜索框 + 分类筛选 +「导入 Excel」+「新增题目」
- 列表：虚拟滚动（支持上万道题），每行 = 题号 + 分类 + 题干摘要 + 答案 + 编辑/删除
- 新增/编辑：表单弹窗（一级分类下拉 + 二级分类下拉 + 题干 + A/B/C/D + 正确答案 + 解析）
- Excel 导入流程：选文件 → 主进程解析 .xlsx（node-xlsx）→ **预览弹窗**（显示有效题目数 + 错误行列表，可修正）→ 确认导入 → 按「题干+选项」去重后写入
- 删除题目：**级联清理**该题的错题记录、收藏、笔记、草稿（弹窗中明示）

### 4.9 设置
- 字号大小：小 / 标准 / 大 / 特大（4 档，即时预览生效，持久保存）
- 配色：日间 / 夜间（即时切换，CSS 变量）
- 数据位置：显示 SQLite 文件路径（只读展示）

---

## 5. 每个模块第一版最必要的数据和操作

| 模块 | 必要数据 | 核心操作 |
|---|---|---|
| 首页 | 累计答题数、正确率、今日答题数、错题数、专项进度、进行中会话 | 跳转各模块、继续上次练习 |
| 专项练习 | 分类树（6 一级 + 政治理论 8 子项）、题目总数/已做数 | 展开折叠、去练习、自定义组卷 |
| 做题页 | 题目列表、用户作答、计时、用时、收藏状态 | 选答案、切换收藏、笔记、草纸、交卷 |
| 结果页 | 每题对错、答案解析 | 查看详情、错题重练 |
| 错题 | 错题列表（错题次数/时间/分类） | 复盘、移除、错题重练 |
| 笔记 | question_id、content（HTML）、时间戳 | 新增/编辑/查看（绑定题目） |
| 收藏 | question_id、时间戳 | 切换收藏、收藏练习、取消收藏 |
| 题目管理 | 题目全文、分类、答案、解析 | 列表、搜索、新增、编辑、删除、Excel 导入 |
| 设置 | fontSize、theme | 调整字号/主题 |

---

## 6. 首页、专项练习和各模块之间的关系

```
首页 ──→ 专项练习目录 ──→ 去练习 ──→ 做题页 ──→ 交卷 ──→ 结果页 ──→ 错题重练 ──→ 做题页
 │                                    │
 │                          （答错的题自动进）  │
 ↓                                    ↓
 错题 ──→ 复盘/移除                错题 ←──────────────────────────────┘
  │                                      │
  │                                      │（可草纸/笔记复盘）
  ↓                                      ↓
 收藏 ←─ 做题页星标                  题目详情（弹窗）
  │                                      │
  ↓                                      │
 收藏练习 ──→ 做题页              笔记（contenteditable，1秒自动保存）
  │
  ↓
 笔记汇总 ←─ 做题页笔记按钮
```

**进度保留机制**：
- 进行中会话全局唯一（同一时刻只存在一个未完成的练习）
- 每选一题、每过 10 秒自动保存到 SQLite
- 做题页返回 = 保存进度后离开，不丢失
- 首页显示「继续上次练习」按钮，恢复题目顺序、已选答案、累计用时
- 新开练习时若存在进行中会话，弹确认「开始新的将覆盖旧进度」

---

## 7. 本地数据文件、备份和恢复要求

### 数据存储
- **所有数据统一存 SQLite 数据库**（题库、错题、收藏、笔记、草稿笔迹、练习记录、设置）
- 数据库路径：`C:\Users\{用户名}\AppData\Roaming\comate\comate.db`
- 使用 Node 内置 `node:sqlite`（Electron 43 / Node 24 可用），无需原生模块编译
- 渲染进程不直接访问 fs/数据库，全部通过 IPC 交由主进程操作

### SQLite 表结构

| 表名 | 主键 | 用途 |
|---|---|---|
| `categories` | id | 分类树（6 一级 + 政治理论 8 子项） |
| `questions` | id | 题目：category_id, type, stem, options(JSON), answer, analysis |
| `question_progress` | question_id | 每题答题次数/正确次数/最后作答时间 |
| `wrong_records` | question_id | 错题本：错题次数/最后答错时间/是否移除 |
| `favorites` | question_id | 收藏及收藏时间 |
| `notes` | question_id | 笔记：HTML 内容，时间戳 |
| `drafts` | question_id | 草稿笔迹：JSON 路径数组（tool/points/lineWidth/color） |
| `practice_sessions` | id | 练习会话：type/title/status/questions_json/answers_json/result_json/duration_ms |
| `settings` | key | 设置：fontSize / theme |

### 草稿笔迹特殊处理
- 数据模型：每笔记录 `{ tool: 'pen'|'eraser', points: [{x,y}...], lineWidth, color }`
- **存路径数组，不存 base64 图片**
- 撤销/重做：对路径数组做双栈管理，不操作 canvas 像素
- 每次落笔/擦除完成后经 IPC 保存；「清空」删除该题草稿；「退出」仅关闭弹窗保留数据
- 重启后打开同一道题草纸，路径数组从库中取出，在 canvas 上完整重绘笔迹

### 备份与恢复
- 第一版**暂不提供**内置备份/恢复功能
- 备份方式：手动复制 `comate.db` 文件
- 数据位置在设置页显示路径（只读），供用户手动备份参考

---

## 8. 第一版必须完成的功能

1. **侧边栏导航** + 7 个页面路由
2. **首页**：4 个统计卡 + 专项进度条 + 快捷操作 + 最近笔记 + 继续上次练习
3. **专项练习目录**：树形折叠（政治理论 8 子项 + 5 一级分类，三角箭头展开收起，每行「已做/总题数 + 去练习 >」）
4. **自定义组卷**：分类多选 + 题量 10/20/30/50
5. **做题页**：20 题/次，顶部返回/计时/标题/设置/交卷，单选题卡片，圆形单选框
6. **进度保留**：选题自动保存、返回不丢失、「继续上次练习」恢复
7. **交卷**：确认弹窗 → 统一判分 → 结果页 → 答错自动进错题本
8. **PaperCanvas 草纸浮层**：canvas 画笔/橡皮、路径数组 IPC 持久化、撤销/重做、清空/退出、重启后笔迹恢复
9. **富文本笔记编辑器**：contenteditable（加粗/斜体/标题/列表/引用），停止输入 1 秒自动保存，HTML 格式存库
10. **错题**：列表/筛选/搜索/复盘（详情弹窗含草纸+笔记）/移除/错题重练
11. **收藏**：星标收藏/取消、收藏练习
12. **笔记汇总页**：按时间倒序列出全部笔记，点击跳转题目详情
13. **题目管理**：列表/分页/搜索/筛选/新增/编辑/删除 + Excel(.xlsx)导入（含预览/错误报告/去重）
14. **设置**：字号 4 档 + 日间/夜间主题，即时生效并持久化
15. **首页/错题/收藏练习共用做题页和结果页**（来源 type 不同，标题不同）
16. **错题重练时卡片右上角显示「🗑移出错题本」按钮**（仅在错题重练来源时显示，点击确认后置灰）
17. **分类种子数据**：6 个一级分类 + 政治理论 8 个子项 + 若干示例题（12 道）
18. **Electron 43 + node:sqlite + electron-vite 三层架构**，渲染进程零 fs 访问
19. **生产构建通过**（`pnpm build` 输出 out/ 目录），启动脚本正确处理 `ELECTRON_RUN_AS_NODE` 环境变量

---

## 9. 第一版暂时不做的功能

1. **多题型**：多选题、判断题、简答题等（数据表 `type` 字段已预留）
2. **题干/选项图片渲染**：数据结构已按 HTML 存储（`v-html` 渲染），兼容未来在 Excel 里写图片链接的做法（第一版题库全部纯文本）
3. **富文本编辑器工具栏的插入图片功能**
4. **数据备份导出**（「下载」按钮已按需求删除）
5. **自定义刷题中的题型筛选**（v1 只有单选）
6. **其他 5 个一级分类的子分类**（目录结构预留，可在配置中扩展）
7. **首页复杂图表**（统计卡片 + 进度条已满足需求）
8. **移动端适配**
9. **联网、云同步、登录注册**
10. **错题重练时已答对题自动移出错题本**（改为手动「标记已掌握」）
11. **做题中标记「跳过」题**（v1 未作答 = 跳过，交卷时计入未答）

---

## 10. 验收标准

### 数据层
- [ ] SQLite 数据库文件位于 `AppData\Roaming\comate\comate.db`
- [ ] 13 个分类（6 一级 + 政治理论 8 子项）已种子写入
- [ ] 12 道示例题可正常显示和作答
- [ ] 渲染进程无任何 `require('fs')` 或 `require('node:fs')` 调用（代码审查验证）

### 首页
- [ ] 累计答题/正确率/今日答题/错题数四个统计卡数值正确（做题后实时变化）
- [ ] 专项进度条比例与「已做/总题数」一致
- [ ] 「继续上次练习」在存在进行中会话时显示，点击恢复做题进度

### 专项练习目录
- [ ] 政治理论展开后显示 8 个子项 + 1 个「全部」
- [ ] 其他 5 个分类可展开（显示「暂无子分类」）
- [ ] 每行「已做/总题数」数字正确
- [ ] 「自定义刷题」弹窗分类可多选、题量可选、开始后进入做题页

### 做题页
- [ ] 每次抽取 20 道题，随机不重复
- [ ] 计时器进入即开始，退出时暂停
- [ ] 选答案后卡片样式变化，选错可改选
- [ ] 返回后重新进入，答案状态保留
- [ ] 交卷确认弹窗显示已答/未答数
- [ ] 交卷后跳结果页，答错的题进入错题本

### 草纸 PaperCanvas
- [ ] 画笔在 canvas 上绘制，线条颜色/宽度正确
- [ ] 橡皮擦除有效
- [ ] 撤销/反撤销基于路径数组栈，不操作像素
- [ ] 落笔后 IPC 保存，刷新页面/重启后笔迹完整恢复
- [ ] 「清空」删除草稿数据；「退出」仅关闭弹窗，数据保留
- [ ] Ctrl+Z 撤销、Ctrl+Y 反撤销、Esc 退出

### 笔记
- [ ] 富文本编辑器工具栏（加粗/斜体/标题/列表/引用）均有效
- [ ] 停止输入 1 秒后自动保存，底部显示「已保存」
- [ ] 刷新页面后笔记内容保留
- [ ] 笔记汇总页显示全部笔记，按更新时间倒序

### 错题
- [ ] 交卷答错的题自动出现在错题列表
- [ ] 分类筛选和搜索有效
- [ ] 复盘弹窗显示题目详情、解析、旧笔迹、笔记
- [ ] 「移出错题本」操作有效
- [ ] 错题重练随机抽题，答对不自动移除

### 收藏
- [ ] 做题页星标点击即收藏/取消
- [ ] 收藏列表正确，收藏练习随机抽题

### 题目管理
- [ ] 题目列表分页加载，搜索和分类筛选有效
- [ ] 新增/编辑题目表单数据正确保存
- [ ] 删除题目时级联清理相关数据（弹窗中有提示）
- [ ] Excel 导入：解析 .xlsx 有效，预览显示题目数量，错误行报告准确，按题干去重

### 设置
- [ ] 字号 4 档切换后页面字体即时变化
- [ ] 日间/夜间主题切换后全站配色即时变化
- [ ] 刷新页面后设置保持

### 端到端流程
- [ ] 全流程（组卷→做题→保存进度→返回→继续→交卷→结果→错题重练→草纸复盘→笔记→收藏）数据链路不断
- [ ] `pnpm build` 生产构建成功，输出 `out/` 目录
- [ ] `pnpm dev` 正常启动，无主进程崩溃，无渲染层 Vue 报错

---

## 附录：技术栈与环境要求

| 项目 | 版本/要求 |
|---|---|
| Node.js | v24+（需内置 `node:sqlite` 模块） |
| pnpm | 11.x |
| Electron | 43.x（内置 Node 24） |
| electron-vite | 5.x |
| Vue | 3.5+ |
| vue-router | 5.x（使用 `createWebHashHistory`） |
| pinia | 4.x |
| xlsx | 0.18.x（Excel 解析） |
| 数据库 | `node:sqlite`（内置，无需 npm 包） |
| 存储 | SQLite，路径 `AppData\Roaming\comate\comate.db` |

**已知环境问题**：系统环境变量 `ELECTRON_RUN_AS_NODE=1` 会导致 Electron 无法加载内置模块。已在 `package.json` 启动脚本中临时清空（`set ELECTRON_RUN_AS_NODE=`），不影响系统环境。

---

*最后更新：2026-08-25，v1.0 初版交付*
